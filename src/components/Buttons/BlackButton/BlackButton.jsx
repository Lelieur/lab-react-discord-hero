import "./BlackButton.css"

const BlackButton = ({ text }) => {
    return (
        <div className="BlackButton">
            <button>{text}</button>
        </div>
    )
}

export default BlackButton